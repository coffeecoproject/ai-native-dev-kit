{
  "schemaVersion": "1.0",
  "artifactType": "eval",
  "required": ["schema_version", "artifact_type", "number", "slug", "title", "spec", "status", "created_at"],
  "properties": {
    "schema_version": { "type": "string", "pattern": "^1\\.0$" },
    "artifact_type": { "type": "string", "enum": ["eval"] },
    "number": { "type": "string", "pattern": "^[0-9]{3}$" },
    "slug": { "type": "string" },
    "title": { "type": "string" },
    "spec": { "type": "string" },
    "status": { "type": "string", "enum": ["draft", "ready", "done"] },
    "created_at": { "type": "string" }
  }
}
