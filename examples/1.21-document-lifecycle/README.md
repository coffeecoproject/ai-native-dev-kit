# 1.21 Document Lifecycle Example

This example shows how an existing project can record a document lifecycle
review without deleting, moving, archiving, or rewriting files.

Run:

```bash
node ../../scripts/check-document-lifecycle.mjs .
```

The report treats archive as a suggestion and keeps source-of-truth changes as
human decisions.
