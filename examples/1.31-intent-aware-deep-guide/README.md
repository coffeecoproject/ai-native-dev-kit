# 1.31 Intent-Aware Deep Guide Example

This example shows `guide --deep --intent`.

The intent is "我要加支付预约". Codex should combine the project state with the user goal, classify payment/value-transfer risk, select read-only checks, and return one Workflow Guidance Card.

The example remains non-authorizing: no implementation, CI change, hook installation, task state change, release, or production approval is granted.
