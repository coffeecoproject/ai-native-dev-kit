# Goal Mode First Route Example

This example shows Goal Mode as a route-selection artifact before implementation.

The Goal Card chooses `DEFINE_WORK`, lists the required request/preflight/spec/eval/task path, and explicitly states that the card is not implementation approval.

Check it with:

```bash
node scripts/check-goal-mode.mjs examples/goal-mode-first-route
```
