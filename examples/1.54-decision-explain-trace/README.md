# 1.54 Decision Explain Trace Example

This example shows a Closure Decision that is not done because a contract approval rule still needs human confirmation.

The important part is not only the final decision. The record also explains:

- the input that controls the final result
- why passing inputs cannot override that input
- what conflict, if any, exists between closure inputs
