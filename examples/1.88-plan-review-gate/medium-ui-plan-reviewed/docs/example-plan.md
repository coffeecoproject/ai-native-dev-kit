# Implementation Plan

Intent: update the appointment list UI and API response label.

Scope:
- Update one list label.
- Keep the API response shape stable.
- Use existing project-native verification command: `npm run verify`.

Boundaries:
- Account rules stay unchanged.
- Storage structure stays unchanged.
- Launch path stays unchanged.
