# Implementation Plan

Intent: update a business approval rule.

This plan contains `STALE_PLAN_MARKER` to model a plan whose digest or source
binding no longer matches the current task.

Verification command to review later: `npm run verify`.

