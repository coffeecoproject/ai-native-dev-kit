# 1.17 Guided Baseline Selection Examples

These examples show plain-language Baseline Decision Cards.

They are simulated examples. They do not approve target-project writes, implementation, release, production, or BL2 activation.

Run:

```bash
node scripts/check-guided-baseline-selection.mjs examples/1.17-guided-baseline-selection/<example> --strict
```
