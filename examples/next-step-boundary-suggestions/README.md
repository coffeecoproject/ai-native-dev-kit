# Next-Step Boundary Example: Suggestion Types

This example shows all allowed next-step suggestion types in one final report.

Validation:

```bash
node scripts/check-next-step-boundary.mjs examples/next-step-boundary-suggestions
```
