# 1.63 Native Migration Precision Example

This example shows how an existing project can be reviewed for IntentOS-native migration without collapsing old project rules into one broad workflow claim.

The strict checker validates:

- rule extraction coverage
- source line ranges
- context headings
- parser warnings
- machine-readable evidence
- summary and JSON consistency

