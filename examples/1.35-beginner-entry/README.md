# 1.35 Beginner Entry Example

This example shows the default beginner-facing card for a user who only says:

```text
我想做一个预约 App
```

The card does not require the user to know internal workflow commands. It does not approve writing files.
