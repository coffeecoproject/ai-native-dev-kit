# 1.83 Task Governance Examples

These examples show behavior-complete existing-project adoption without forcing
full native asset migration.

Each report classifies a task as `LOW`, `MEDIUM`, `POSSIBLE_HIGH`, or `HIGH`.
The report routes required governance but does not authorize implementation,
commit, push, release, production, CI, hooks, tests, or migrations.

