# Release SOP

This example keeps GitHub as source and evidence only. Release review happens
through a project owner process outside GitHub Release assets and GitHub Actions
artifact packages.

Required release review inputs:

- release owner approval;
- current task completion evidence;
- rollback note;
- preserved release evidence outside the runtime bundle.
