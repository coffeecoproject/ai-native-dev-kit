# 1.34 Unified Apply Plan Example

This example shows a plan-only existing-project adoption apply plan.

The plan proposes workflow bridge documentation and explicitly does not write files, authorize apply, replace existing governance, modify CI/hooks, or approve implementation/release.
