# 1.6 Conversation Drift Control Example

This example shows a user side question that is classified as discussion-only, plus a separate scope change report.

Run:

```bash
node scripts/check-conversation-drift.mjs examples/1.6-conversation-drift-control
```
