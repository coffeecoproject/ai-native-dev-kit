# 1.25 Review Surface Governance Example

This example shows how Codex records what must be reviewed before and after a task.

The sample is an appointment app task. Codex selects review surfaces instead of asking the user to choose technical review types.

Run:

```bash
node ../../scripts/check-review-surface.mjs .
```

Expected result:

```text
Review surface check passed.
```
