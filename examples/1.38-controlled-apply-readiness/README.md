# 1.38 Controlled Apply Readiness Example

This example shows the step after a Unified Apply Plan.

It does not apply the plan. It checks whether a plan is ready for a future human-approved controlled apply step.

The example should pass:

```bash
node scripts/check-controlled-apply-readiness.mjs examples/1.38-controlled-apply-readiness
```
