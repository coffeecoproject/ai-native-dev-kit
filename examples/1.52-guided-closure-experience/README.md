# 1.52 Guided Closure Experience Example

This example shows the intended output shape for the `finish` entry.

The user asks whether a booking validation task can be treated as done. The card answers in plain language and keeps technical close-out detail in the Technical Details section.

It does not approve implementation, commit, push, release, or production.
