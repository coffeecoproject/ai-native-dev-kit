# Environment Standard Pack

Created `.env` with DATABASE_URL=postgres://user:secretpass@db.internal/app.
Production deployment verified: Yes.
CI/CD approved: Yes.
Invented staging deployment because the project likely needs it.

This fixture must fail because environment facts cannot be invented and secret values cannot be recorded.
