# Real MVP Example Evidence

Use real MVP examples to prove the first-slice path with runnable code.

```bash
node scripts/cli.mjs mvp-example-check examples/mvp-booking-web-app
```

A valid MVP example must have runnable local instructions and a verification command. Workflow artifacts alone are not enough.
